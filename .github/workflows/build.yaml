name: Build and publish distroless-lldap

on:
  push:
    branches: [ "main" ]
    paths:
      - Containerfile
      - versions.yaml
  pull_request:
    branches: [ "main" ]
    paths:
      - Containerfile
      - versions.yaml
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - name: Load versions
        run: |
          echo "LLDAP_VERSION=$(yq -r '.lldap' versions.yaml)" >> $GITHUB_ENV

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to GHCR
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        run: |
          docker buildx build \
            --cache-from type=gha \
            --cache-to type=gha,mode=max \
            --build-arg LLDAP_VERSION=${LLDAP_VERSION} \
            -t ghcr.io/simons-containers/distroless-lldap:${LLDAP_VERSION} \
            -t ghcr.io/simons-containers/distroless-lldap:latest \
            --push \
            -f Containerfile .
